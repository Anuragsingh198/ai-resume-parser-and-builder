# JobHunch Frontend

React/Next frontend for extracting structured job data from job URLs or pasted descriptions.

## Setup

```bash
cd frontend
npm install
cp .env.example .env.local
```

## Run

```bash
npm run dev
```

Open `http://localhost:3000`.

The backend should be running at `http://127.0.0.1:5000`.
